{
  "name": "portfolio-backend",
  "version": "1.0.0",
  "description": "Portfolio website backend API",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "npm install && npm install -g nodemon && npm install -g pm2",
    "dev": "nodemon server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "test:db": "node -e \"require('./database')().then(() => process.exit(0)).catch(() => process.exit(1))\"",
    "seed": "node seed.js"

  },
  "keywords": [
    "portfolio",
    "backend",
    "api"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.6.1",
    "express": "^4.18.2",
    "helmet": "^7.1.0",
    "mongodb": "^6.18.0",
    "mongoose": "^8.17.1",
    "morgan": "^1.10.0",
    "nodemailer": "^7.0.5"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}
